# 前端嵌入

将前端构建产物放入 `dist/` 目录，程序会自动通过 `go:embed` 嵌入到二进制文件中。

## 使用方式

1. 编译前端项目，生成 `dist/` 目录
2. 将 `dist/` 复制到本目录
3. 重新编译 Go 项目即可

或使用 `make web` 自动完成上述步骤。

## 运行时回退

如果嵌入的 dist 为空（开发模式），程序会自动尝试从外部 `./dist/` 目录加载前端文件。
